## 2024-04-12 - Adding loading spinner to Save Routine
**Learning:** Found that saving routines lacks visual feedback when loading, especially on mobile since it only uses the icon. Adding a spinner on `isSaving` improves UX.
**Action:** Adding a loading spinner to the save button in RoutineView.
