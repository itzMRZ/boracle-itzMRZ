## 2024-05-01 - Missing ARIA Labels on Icon-only buttons
**Learning:** Found multiple instances where interactive, icon-only `<button>` components lack `aria-label` attributes.
**Action:** Adding `aria-label` attributes to specific icon-only buttons globally to ensure full accessibility support for screen readers.
